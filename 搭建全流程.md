# InboxTriage 全流程搭建手册（零本金版）

> 一句话总览：**别人给你一个 16 位应用专用密码 → 你的 Python 脚本用 IMAP 只读拉他邮件 → 丢给 Gemini 免费 API 分类+写草稿+抽待办 → 结果存 Supabase → Next.js 页面展示 → Vercel 部署并每小时自动跑。全程 0 元。**

---

## 0. 先搞清楚三个「谁」

| 问题 | 答案 |
|---|---|
| 谁来搭？ | 你 + 我。代码我写，账号注册和点确认你来（因为要你的邮箱/手机） |
| 用谁的 AI？ | **Google Gemini 2.0 Flash**，免费额度每天 1500 次请求，够你前 50 个客户用。不用 Claude（贵）、不用 OpenAI（要绑卡） |
| 怎么碰别人邮箱？ | **不用 OAuth**。用 Gmail「应用专用密码 + IMAP」。用户自己生成一串 16 位码给你，随时能撤销，Google 不审你 |

---

## 1. 为什么不用 Google OAuth（必须理解，否则你会白干两周）

读用户 Gmail 正文 = Google 的 **Restricted Scope（受限权限）**。
要给陌生人正式使用，Google 强制要求：

1. 公司主体 + 官网 + 隐私政策页
2. 提交 OAuth 验证（等 2–6 周）
3. **CASA 第三方安全审计，$500 起，每年复审**

你零本金 + 未成年 → 这条路当前走不通。

**替代方案（100% 合法、Google 官方支持）：应用专用密码 + IMAP**

- 用户不给你真实密码，给的是一串只能用于收信的 16 位专用码
- 用户在 Google 账号页面点一下就能吊销
- 只读（IMAP 只拉不发），完全符合你「绝不自动发邮件」的定位
- 不需要任何审核、不花一分钱

代价：用户要多花 2 分钟自己去生成。所以你要给他一份**图文教程**（见第 6 节，文案我已经写好）。

---

## 2. 完整工具清单（全部免费档）

| 环节 | 用什么 | 花钱吗 | 谁注册 |
|---|---|---|---|
| 落地页托管 | Netlify Drop | 免费 | 你（邮箱即可） |
| 收邮箱表单 | Tally.so | 免费 | 你 |
| 拉邮件 | Python `imaplib`（标准库） | 免费 | 无需注册 |
| AI 分析 | Google AI Studio → Gemini API Key | 免费 | 你（普通 Google 账号，不用绑卡） |
| 数据库 | Supabase | 免费 500MB | 你 |
| 前端 | Next.js 14 | 免费 | — |
| 部署 + 定时任务 | Vercel（Cron 每小时） | 免费 | 你（GitHub 登录） |
| 收款 | Lemon Squeezy | 抽成 5% | **你爸实名** |

**一分钱不用出。唯一要成年人的只有最后收款那一步。**

---

## 3. 执行顺序（严格按 1→2→3，不要跳）

### 阶段 A：验证有没有人给钱（本周，0 行代码）

**A1.** 打开 tally.so，用邮箱注册 → New form → 只留一个字段 `Email` → 标题写 `InboxTriage — Early Access` → 发布，复制表单链接。

**A2.** 我把落地页的两个表单换成 Tally 链接（你把链接发我，我改）。

**A3.** 打开 app.netlify.com/drop，把 `inbox-triage` 这个文件夹直接拖进去 → 几秒后拿到网址，形如 `xxx.netlify.app`。**这就是你的产品官网。**

**A4.** 去这几个地方发帖（免费流量，都是老外小卖家聚集地）：
- Reddit：r/Etsy、r/EtsySellers、r/freelance、r/smallbusiness
- Indie Hackers
- X（Twitter）发一条带截图的

发帖别硬广，写法：「我一天 30 封邮件全是广告混客户咨询，快疯了，做了个自动分拣的小工具，想找 10 个人免费试用，愿意的评论区扣 1」

**A5.** 目标：**收到 10 个邮箱**。收不到 10 个 → 需求不成立，停手，别写代码。收到了 → 进阶段 B。

---

### 阶段 B：手动交付前 5 个人（第 2 周，还是几乎不写代码）

不要一上来就做产品。你先当「人肉版产品」，这叫 concierge MVP。

**B1.** 给报名的人发这封信（原文可直接复制）：

> Hi — thanks for signing up to InboxTriage.
>
> I'm onboarding the first users manually so I can tune the AI to your actual inbox. Here's the deal: **7 days, completely free, no card.**
>
> Every morning you'll get one email from me with:
> 1. Your inbox sorted into Spam / Customer / Lead / High-risk
> 2. A ready-to-send reply draft for every customer email
> 3. Every deadline and promise pulled out into a task list
>
> To do this I need **read-only** access. I never see or store your password — Google gives you a separate 16-character code that only works for reading mail, and you can revoke it in one click anytime. I will never send email from your account.
>
> Setup takes 2 minutes: [教程链接]
>
> Reply with the 16-character code and your Gmail address and I'll start tomorrow morning.

**B2.** 收到码之后，你在本地跑我给你的脚本，生成报告，手动发给他。5 个人 × 7 天 = 你花每天 20 分钟。

**B3.** 第 7 天问一句：`Want me to keep this running? It's $12/month.`
- 有 ≥2 个人说 yes → 进阶段 C，开始真做产品
- 全都不续 → 问清楚为什么，改产品或者换方向。**你只损失了两周，0 元。**

---

### 阶段 C：自动化（有人付钱后才做）

技术架构（我全部帮你写代码）：

```
Vercel Cron（每小时触发）
    ↓
Next.js API Route /api/cron
    ↓
遍历 Supabase 里所有付费用户
    ↓
imaplib 连 imap.gmail.com:993（SSL）
用 用户邮箱 + 应用专用密码 登录
拉取最近 24 小时未处理邮件
    ↓
每封邮件 → Gemini 2.0 Flash（一次调用同时输出 JSON）
    { category, urgency, reply_draft, tasks:[{title,due_date}] }
    ↓
写入 Supabase 表 emails / tasks
    ↓
用户登录 Next.js 前端 → 看四栏看板 + 待办清单 + 一键复制草稿
```

数据库三张表：
- `users`：id, email, gmail_address, app_password（加密存）, plan, created_at
- `emails`：id, user_id, gmail_uid, subject, from, snippet, category, reply_draft, created_at
- `tasks`：id, user_id, email_id, title, due_date, done

---

## 4. Gemini API 怎么拿（3 分钟，免费无需绑卡）

1. 打开 `aistudio.google.com`
2. 用你自己的 Google 账号登录
3. 左侧点 **Get API key** → Create API key
4. 复制那串 `AIza...` 开头的密钥，发我，我写进代码

免费额度：Gemini 2.0 Flash 每分钟 15 次、每天 1500 次请求。
按每个用户每天 30 封邮件算 → **免费额度能撑 50 个用户**。真到 50 个用户你月入 600 刀了，那时候再付费也不迟。

---

## 5. 怎么「拉」邮件——技术细节

用 Python 标准库，不需要装任何 Google SDK：

```python
import imaplib, email
from email.header import decode_header

M = imaplib.IMAP4_SSL("imap.gmail.com", 993)
M.login("客户邮箱@gmail.com", "abcd efgh ijkl mnop")  # 16位应用专用密码
M.select("INBOX", readonly=True)   # readonly=True → 物理上不可能改动他邮箱

status, data = M.search(None, '(SINCE "30-Jul-2026")')
for uid in data[0].split():
    _, msg_data = M.fetch(uid, "(RFC822)")
    msg = email.message_from_bytes(msg_data[0][1])
    subject = decode_header(msg["Subject"])[0][0]
    sender  = msg["From"]
    body    = 取正文纯文本
    # → 丢给 Gemini
M.logout()
```

关键点：
- `readonly=True` 意味着**代码层面就不可能发信、不可能删信**。这句话你要写进你的隐私说明，是信任卖点。
- Gmail 前提：用户账号必须开了**两步验证**（否则 Google 不给生成应用专用密码）。

---

## 6. 给客户看的「怎么给我权限」教程（英文原文，直接用）

> **How to connect your Gmail (2 minutes, read-only)**
>
> You will **not** give me your password. Google generates a separate 16-character code that can only read mail, and you can delete it anytime.
>
> 1. Go to **myaccount.google.com/security**
> 2. Turn on **2-Step Verification** if it's not already on (required by Google)
> 3. Go to **myaccount.google.com/apppasswords**
> 4. In the name box type `InboxTriage`, click **Create**
> 5. Google shows a 16-character code like `abcd efgh ijkl mnop` — copy it
> 6. Send me that code + your Gmail address
>
> **To revoke access at any time:** go back to myaccount.google.com/apppasswords and click the trash icon next to `InboxTriage`. Access stops instantly.
>
> What I can do with this: read your inbox.
> What I cannot do: send email, delete email, change your password, access Drive/Photos/anything else.

---

## 7. 现在轮到你做的事（只有 3 件，别的我全包）

1. 去 **tally.so** 建表单，把链接发我
2. 去 **aistudio.google.com** 拿 Gemini API key，发我
3. 把 `inbox-triage` 文件夹拖到 **app.netlify.com/drop**，把网址发我

做完这三件，我就把落地页收尾 + 把阶段 B 用的本地脚本写好给你，你当天就能开始收邮箱。
