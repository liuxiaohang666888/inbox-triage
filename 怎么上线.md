# InboxTriage 上线手册（照着点就行，不需要懂代码）

---

## 先回答你三个问题

### 1. 网址（URL）—— 还差什么？
**差最后一步：把这套代码放到一台 24 小时开着的远程电脑（叫 Python 主机）上。**
现在 `app.py` 只跑在你自己电脑，别人打不开。放到公网主机后，陌生人打开网址、自己填 Gmail、自己出报告。

我已经把"能部署"要的全部文件备好了：
- `app.py`（网页应用本体）
- `requirements.txt`（依赖清单）
- `render.yaml`（主机启动配置）
- `.gitignore`（防泄密，不会把密码传上去）

**这一步必须你本人做**，因为要注册你自己的主机账号——我没法替你登。但你照下面点，20–40 分钟出网址。

### 2. 数据库 —— 搞好没有？
**没搞，而且 v1 根本不需要。**
报告是用户一点"Run"实时生成的，不往数据库里存。所以现在没有数据库也能跑、能卖。
等你想要"历史记录 / 登录后能看上次报告 / 多设备同步"时，再接 Supabase（免费、国际、不用国内实名）。那是有了客户之后的升级，不是现在。

### 3. 收钱（PayPal 订阅）—— 还差什么？
**缺"商户账号"，这个必须你爸（监护人）实名才能开，我开不了。**
原因：你美国版 PayPal 之前用 VPN+假信息注册被封了；收美元订阅得用正规商户。两条正规路（都要监护人实名）：
- **Lemon Squeezy**（最推荐，专门给 SaaS 做订阅，$12/月，自动处理税费，无月费）→ 你爸注册，建一个 $12/月产品，拿到结算链接
- **PayPal 中国（贝宝）**（独立账号系统，不牵连被封的美版，可收境外美元）→ 你爸实名后开"订阅按钮"

**我已经把收钱接口留好了**：网页上那个绿色 "Subscribe — $12/mo" 按钮，指向环境变量 `PAYMENT_URL`。你爸开好商户、拿到链接后，填进主机后台的 `PAYMENT_URL` 一行，按钮就自动指向付款页。**代码不用改。**

> 为什么不直接用美国 PayPal 自动订阅：你美版已被封，且未成年人开不了收美元商户。走监护人实名是唯一的合规路，别绕。

---

## 上线步骤（选一条走）

### 路线 A：Render（推荐，一次配置后自动运行，将来最稳）

1. 打开 **github.com** → 注册（免费）→ 右上角 `+` → **New repository** → 名字填 `inboxtriage` → 选 **Public** → 点 Create
2. 在你电脑上把 `inbox-triage` 文件夹上传到这个仓库（网页上直接拖文件进去也行，不用学 git 命令）
3. 打开 **render.com** → 用 GitHub 登录 → **New + Web Service** → 选刚才那个 `inboxtriage` 仓库
4. Render 会自动读 `render.yaml`，你基本不用填。确认：
   - Build Command：`pip install -r requirements.txt`（自动）
   - Start Command：`gunicorn --bind 0.0.0.0:$PORT app:app`（自动）
5. 展开 **Advanced → Environment Variables**，手动加两行：
   - `GEMINI_API_KEY` = 你的 Gemini key（去 aistudio.google.com 拿，免费）
   - `PAYMENT_URL` = 你爸开好的 Lemon Squeezy / 贝宝 结算链接（没开就先空着，按钮显示"即将开放"）
6. 点 **Create Web Service** → 等 2 分钟 → 它给你一个网址 `https://inboxtriage.onrender.com`
7. 打开那个网址，填你自己的 Gmail + 应用密码，点 Run，看报告出来 → 成了

### 路线 B：PythonAnywhere（免 GitHub，适合不想碰 git 的人）

1. 打开 **pythonanywhere.com** → 注册免费账号
2. 左侧 **Web** → **Add a new web app** → 选 **Flask** → Python 3.10 → 下一步
3. 左侧 **Files** → 进 `/home/你的名/inboxtriage/` → 把 `inbox-triage` 文件夹里的文件全传上去（含 `engine/` 子文件夹）
4. 左侧 **Web** → 点 **WSGI file** 链接 → 把里面内容改成：
   ```python
   import sys
   path = "/home/你的名/inboxtriage"
   if path not in sys.path: sys.path.append(path)
   from app import app as application
   ```
5. 同页面设环境变量（或用一个 `config.json` 把 key 填进去，但别公开仓库）
6. 点 **Reload** → 给你一个 `你的名.pythonanywhere.com` 网址 → 打开测试

---

## 上线后你要干的

1. 把网址发到 Threads / Facebook 群 / Bluesky（之前写好的引流文案直接用）
2. 有人填 Gmail 跑 → 满意 → 点 Subscribe → 你爸的商户收到 $12
3. 前几十个用户用 Gemini 免费额度（1500次/天）完全够，不花钱
4. 等真有稳定收入，再考虑：自定义域名（Namecheap 约 70 元/年）、Supabase 数据库、定时自动跑

---

## 安全红线（务必记住）
- `config.json` 里**不要再放回真密码/真 key**（已经清空了）。密钥只走主机后台的环境变量。
- 用户给你的 16 位应用密码，代码只用来只读拉信，绝不存储、绝不发信。
- 真接客户前，让你爸把收款商户开好再对外收钱，别收了钱没处放。
